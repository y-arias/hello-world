spring:
  application:
    name: ms-domain-score
  profiles:
    active: dev
  cloud:
    config:
      uri: ${spring-cloud-config.host:http://localhost}:${spring-cloud-config.port:8888}
      enabled: false
management:
  endpoints:
    web:
      exposure:
        include: "*"
